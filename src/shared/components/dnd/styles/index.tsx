import { Box, styled } from '@mui/material'

export const DroppableComponent = styled(Box)(({ theme }) => ({
  width: '100%',
}))

export const DraggableComponent = styled(Box)(({ theme }) => ({
    width: '100%',
  }))
